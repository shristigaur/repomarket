# Backend origin (no trailing /api). OAuth buttons append /api/auth themselves.
VITE_API_URL=http://localhost:5000

# For production, use your Render backend domain:
# VITE_API_URL=https://your-backend-domain.onrender.com
